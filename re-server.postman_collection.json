{
  "info": {
    "name": "RE Server Demo",
    "description": "Pre-built request flow for demoing the Real Estate Server. Set the `baseUrl` collection variable if not running on localhost:7070.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:7070",
      "type": "string"
    },
    {
      "key": "listingID",
      "value": "REPLACE_AFTER_FIRST_GET",
      "type": "string",
      "description": "Paste a real listing _id here after running `GET /listing`."
    },
    {
      "key": "purchaserID",
      "value": "REPLACE_AFTER_REGISTER",
      "type": "string",
      "description": "Paste the id returned by `POST /purchaser` here."
    },
    {
      "key": "propertyID",
      "value": "1",
      "type": "string",
      "description": "Pick any property_id visible in the `GET /property` HTML page, paste it here, then run `GET /property/{propertyID}` repeatedly to see view_count climb."
    }
  ],
  "item": [
    {
      "name": "0. Health check",
      "request": {
        "method": "GET",
        "url": {
          "raw": "{{baseUrl}}/",
          "host": ["{{baseUrl}}"],
          "path": [""]
        },
        "description": "Should return 'Real Estate server is running'."
      }
    },
    {
      "name": "1. Properties",
      "item": [
        {
          "name": "GET /property/{propertyID} (bumps view_count)",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/property/{{propertyID}}",
              "host": ["{{baseUrl}}"],
              "path": ["property", "{{propertyID}}"]
            },
            "description": "Returns the most-recent sale row for this property_id AND atomically increments `view_count` on every row sharing that id. Send this 3× — the `Views` column climbs 1 → 2 → 3."
          }
        },
        {
          "name": "GET /property/postcode/{postcode} (bumps postcode counter)",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/property/postcode/2770",
              "host": ["{{baseUrl}}"],
              "path": ["property", "postcode", "2770"]
            },
            "description": "All historical sales in postcode 2770. On non-empty results, increments `postcode_stats.search_count` for 2770 (see `GET /stats/postcode/2770`)."
          }
        },
        {
          "name": "GET /property (price range)",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/property?minPrice=1000000&maxPrice=2000000",
              "host": ["{{baseUrl}}"],
              "path": ["property"],
              "query": [
                { "key": "minPrice", "value": "1000000" },
                { "key": "maxPrice", "value": "2000000" }
              ]
            },
            "description": "Filter by purchase price range."
          }
        },
        {
          "name": "POST /property (create)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"propertyID\": \"99999999\",\n  \"postcode\": \"2770\",\n  \"propertyPrice\": \"850000\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/property",
              "host": ["{{baseUrl}}"],
              "path": ["property"]
            },
            "description": "Record a new property sale."
          }
        }
      ]
    },
    {
      "name": "2. Listings",
      "item": [
        {
          "name": "POST /listing/seed (bulk 1000)",
          "request": {
            "method": "POST",
            "url": {
              "raw": "{{baseUrl}}/listing/seed",
              "host": ["{{baseUrl}}"],
              "path": ["listing", "seed"]
            },
            "description": "Picks 1000 random properties and creates active listings at +20% of last sale. Idempotent? No — re-running appends more listings. Skip if Atlas already has 1000."
          }
        },
        {
          "name": "GET /listing (all)",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/listing",
              "host": ["{{baseUrl}}"],
              "path": ["listing"]
            },
            "description": "All active listings. Grab a listing _id from the output and paste into the `listingID` collection variable."
          }
        },
        {
          "name": "GET /listing/{listingID}",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/listing/{{listingID}}",
              "host": ["{{baseUrl}}"],
              "path": ["listing", "{{listingID}}"]
            },
            "description": "Listing detail + full price history."
          }
        },
        {
          "name": "POST /listing/{listingID}/price (drop price)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"price\": 450000\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/listing/{{listingID}}/price",
              "host": ["{{baseUrl}}"],
              "path": ["listing", "{{listingID}}", "price"]
            },
            "description": "Append a price entry. If lower than the current latest, `is_discounted` flips to true on the listing."
          }
        }
      ]
    },
    {
      "name": "3. Purchasers",
      "item": [
        {
          "name": "POST /purchaser (register)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Demo Buyer\",\n  \"email\": \"demo@instructor.com\",\n  \"postcodes\": [\"2770\", \"2000\"]\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/purchaser",
              "host": ["{{baseUrl}}"],
              "path": ["purchaser"]
            },
            "description": "Register a new buyer with up to 5 NSW postcode interests. Copy the returned id into the `purchaserID` collection variable."
          }
        },
        {
          "name": "GET /purchaser (all)",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/purchaser",
              "host": ["{{baseUrl}}"],
              "path": ["purchaser"]
            },
            "description": "All buyer accounts."
          }
        },
        {
          "name": "GET /purchaser/postcode/{postcode} (bumps postcode counter)",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/purchaser/postcode/2770",
              "host": ["{{baseUrl}}"],
              "path": ["purchaser", "postcode", "2770"]
            },
            "description": "All buyers interested in postcode 2770 — primitive for listing-alert features. On non-empty results, also increments `postcode_stats.search_count` (same counter as the property-postcode endpoint — both signal interest in the postcode)."
          }
        },
        {
          "name": "GET /purchaser/{purchaserID}",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/purchaser/{{purchaserID}}",
              "host": ["{{baseUrl}}"],
              "path": ["purchaser", "{{purchaserID}}"]
            },
            "description": "Single buyer detail."
          }
        },
        {
          "name": "POST /purchaser/{purchaserID}/interest (add)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"postcode\": \"2480\"\n}",
              "options": { "raw": { "language": "json" } }
            },
            "url": {
              "raw": "{{baseUrl}}/purchaser/{{purchaserID}}/interest",
              "host": ["{{baseUrl}}"],
              "path": ["purchaser", "{{purchaserID}}", "interest"]
            },
            "description": "Add a postcode to this buyer's watch list. 5-max cap enforced by the server."
          }
        },
        {
          "name": "DELETE /purchaser/{purchaserID}/interest/{postcode}",
          "request": {
            "method": "DELETE",
            "url": {
              "raw": "{{baseUrl}}/purchaser/{{purchaserID}}/interest/2480",
              "host": ["{{baseUrl}}"],
              "path": ["purchaser", "{{purchaserID}}", "interest", "2480"]
            },
            "description": "Remove a postcode from this buyer's watch list."
          }
        },
        {
          "name": "POST /purchaser/seed (bulk synthetic)",
          "request": {
            "method": "POST",
            "url": {
              "raw": "{{baseUrl}}/purchaser/seed",
              "host": ["{{baseUrl}}"],
              "path": ["purchaser", "seed"]
            },
            "description": "Bulk-insert synthetic Buyer accounts with random 0-5 postcode interests."
          }
        }
      ]
    },
    {
      "name": "4. Stats (ex5)",
      "item": [
        {
          "name": "GET /stats/postcode/{postcode}",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/stats/postcode/2770",
              "host": ["{{baseUrl}}"],
              "path": ["stats", "postcode", "2770"]
            },
            "description": "Returns `{postcode, searchCount}` from the `postcode_stats` collection. Counter is bumped by `GET /property/postcode/{pc}` and `GET /purchaser/postcode/{pc}` (both = interest signal). Run those a few times, then this, to see the count."
          }
        }
      ]
    },
    {
      "name": "5. Notify",
      "item": [
        {
          "name": "GET /notify (HTML)",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/notify",
              "host": ["{{baseUrl}}"],
              "path": ["notify"]
            },
            "description": "Notification report — for each Buyer with watched postcodes, lists the for-sale properties in those postcodes (id + latest price)."
          }
        },
        {
          "name": "GET /notify?format=text",
          "request": {
            "method": "GET",
            "url": {
              "raw": "{{baseUrl}}/notify?format=text",
              "host": ["{{baseUrl}}"],
              "path": ["notify"],
              "query": [{ "key": "format", "value": "text" }]
            },
            "description": "Plain-text version of the notification report — easier to scan for large outputs."
          }
        }
      ]
    }
  ]
}
